;(function(glob) {
	
	// define static data
	var app_data = {
		name: "vs_pm",
		mode: "normal"
	};
	// create app resources
	if (!glob.app) {
		glob.app = {};
		glob.app.module = {};
	}

})(window);
